name: shadcn_theme
description: "Theme-only Flutter package for shadcn color themes and style presets."
version: 0.1.9
repository: https://github.com/lingjhf/shadcn_theme
issue_tracker: https://github.com/lingjhf/shadcn_theme/issues
documentation: https://github.com/lingjhf/shadcn_theme/tree/master/doc
topics:
  - flutter
  - theme
  - shadcn
  - material
  - design-system
platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

environment:
  sdk: ^3.11.5
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
